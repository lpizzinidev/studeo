import {
  SET_RESOURCES_LIST,
  CREATE_RESOURCE,
  UPDATE_RESOURCE,
  DELETE_RESOURCE,
} from "./ActionTypes";

export const ResourcesReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
