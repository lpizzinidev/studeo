version: '3'
services:
  frontend:
    restart: unless-stopped
    build:
      dockerfile: Dockerfile.prod
    ports:
      - '3000:3000'

  backend:
    restart: unless-stopped

  mongo:
    restart: unless-stopped
